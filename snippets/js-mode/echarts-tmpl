# -*- mode: snippet -*-
# name: echarts-tmpl
# key: echarts-tmpl
# --
import { assert, tryCatchAsync } from 'shared-utils'

async function get(token) {
  assert.nullable(token, 'token')

  const result = await tryCatchAsync(async () => {
    return await
  })

  return
}

export default {
  get,
  name: 'daping-jtsn-airport-24hours-flow-trend'
}
